<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="30dp"
    android:background="@color/white">

    <!-- Only the Logo (since it already has the text/slogan) -->
    <ImageView
        android:layout_width="280dp"
        android:layout_height="280dp"
        android:src="@drawable/onserve_logo"
        android:scaleType="fitCenter" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="60dp">

        <!-- Use android.widget.Button to prevent the purple theme override -->
        <android.widget.Button
            android:id="@+id/loginButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Log in"
            android:textAllCaps="false"
            android:textSize="18sp"
            android:textColor="@color/white"
            android:background="@drawable/button_navy_rounded"
            android:layout_marginBottom="15dp" />

        <android.widget.Button
            android:id="@+id/signupButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Sign Up"
            android:textAllCaps="false"
            android:textSize="18sp"
            android:textColor="@color/navy_onserve"
            android:background="@drawable/button_outline_navy" />
    </LinearLayout>
</LinearLayout>